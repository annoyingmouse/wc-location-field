{
  "name": "@annoyingmouse/wc-location-field",
  "version": "1.0.7",
  "description": "A web component for location selection with address search via Nominatim or Google Maps, an optional interactive map, browser geolocation, and What3Words support.",
  "main": "wc-location-field.js",
  "type": "module",
  "scripts": {
    "format": "biome format --write .",
    "lint": "biome lint .",
    "test": "web-test-runner",
    "test:dist": "web-test-runner --config web-test-runner.dist.config.mjs",
    "build": "rollup -c",
    "release": "npm run build && node scripts/sync-version.cjs && git add -A && git commit -m \"chore: release artifacts\" && git push && git push --tags && npm publish --access public",
    "release:patch": "npm version patch && npm run release",
    "release:minor": "npm version minor && npm run release",
    "release:major": "npm version major && npm run release"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/annoyingmouse/wc-location-field.git"
  },
  "keywords": [
    "web-component",
    "custom-element",
    "location",
    "geocoding",
    "nominatim",
    "google-maps",
    "places-autocomplete",
    "leaflet",
    "geolocation",
    "what3words",
    "accessibility",
    "a11y"
  ],
  "author": "Dominic Myers",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/annoyingmouse/wc-location-field/issues"
  },
  "homepage": "https://github.com/annoyingmouse/wc-location-field#readme",
  "prettier": {
    "singleQuote": true,
    "semi": false
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.9",
    "@open-wc/testing": "^3.2.0",
    "@web/test-runner": "^0.18.0",
    "@web/test-runner-commands": "^0.9.0",
    "@web/test-runner-playwright": "^0.11.0",
    "@rollup/plugin-terser": "^1.0.0",
    "rollup": "^4.0.0"
  }
}
